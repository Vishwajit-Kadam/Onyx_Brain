# Documentation Pack Example

```bash
cargo run -- init
cargo run -- autonomize --level full-bounded "Create a documentation pack for Onyx Brain commands with overview, user guide, command guide, architecture guide, troubleshooting, FAQ, and final report"
cargo run -- artifact-packs
cargo run -- artifact-pack-inspect latest
```

The generator creates local markdown documentation artifacts inside the sandbox/workspace layout.
