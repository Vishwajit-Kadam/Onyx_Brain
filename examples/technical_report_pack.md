# Technical Report Pack Example

```bash
cargo run -- autonomize --level full-bounded "Create a technical report pack about Onyx Brain with architecture summary, component map, safety model, limitations, test plan, risk register, and final report"
```

Onyx writes deterministic markdown documents, local research notes, verification caveats, final audit data, and a report card. It does not perform network research by default.
