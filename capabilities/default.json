{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the Onyx Brain desktop GUI shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
